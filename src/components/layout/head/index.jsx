import { Layout } from 'antd';

const { Header } = Layout;

const Head = () => {
  return (
    <Header className="header">
      <div className="logo" />
      head
    </Header>
  );
};

export default Head;
